# GENERATED FILE — do not edit directly.
# Source: RiddimSoftware/riddim-release templates/workflows/conflict-scan.tpl.yml
# Sync:   scripts/render_shims.py --repo riddim-website
# Conflict Scan — consumer shim
# Fires on push to main. Finds conflicting autonomous PRs and resolves
# them autonomously when the shared agent-loop policy allows it.
name: Conflict Scan
on:
  push:
    branches: [main]
permissions:
  contents: write
  pull-requests: write
  issues: write
jobs:
  conflict-scan:
    uses: RiddimSoftware/agent-loop/.github/workflows/conflict-scan.yml@main
    secrets: inherit
