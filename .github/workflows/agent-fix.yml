# GENERATED FILE — do not edit directly.
# Source: RiddimSoftware/riddim-release templates/workflows/agent-fix.tpl.yml
# Sync:   scripts/render_shims.py --repo riddim-website
# Agent Fix — consumer shim
# Handles pull_request_review events for agent developer fix-up passes.
# Kept in a dedicated file separate from agent-loop.yml to avoid cross-event
# startup_failure when both files listen on pull_request_review.
# All fix logic lives in RiddimSoftware/agent-loop/.github/workflows/agent-fix.yml
name: Developer Fix

on:
  pull_request_review:
    types: [submitted]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  agent-fix:
    uses: RiddimSoftware/agent-loop/.github/workflows/agent-fix.yml@main
    with:
      pr_number: ${{ github.event.pull_request.number }}
    secrets: inherit
